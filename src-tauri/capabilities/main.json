{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capabilities for the Folia main and remote-control windows (shim event listeners)",
  "windows": ["main", "remote"],
  "permissions": ["core:event:default"]
}
